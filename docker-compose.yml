services:
  unofficial-api:
    image: 2noscript/unofficial-api:latest
    ports:
      - "8088:8088"
    env_file:
      - .env
    volumes:
      - unofficial-api-data:/data
    restart: unless-stopped

volumes:
  unofficial-api-data:

