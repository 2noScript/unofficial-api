services:
  unofficial-api:
    build: .
    image: 2noscript/unofficial-api:latest
    ports:
      - "8088:8088"
    env_file:
      - .env
    environment:
      - UNOFFICIAL_API_DATA_DIR=/data
    volumes:
      - ./data:/data
    restart: unless-stopped
